services:

  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - chroma_data:/app/chroma_db   # ChromaDB lokal dosyaları kalıcı sakla
    restart: unless-stopped

volumes:
  chroma_data:
